# Google GenAI / ADK credentials
GOOGLE_API_KEY=api_key

# Sentinel runtime defaults
SENTINEL_MODEL=gemini-3-pro-preview
SENTINEL_CONSTITUTION_PATH=./Sentinel-Constitution.yaml
SENTINEL_AUTH_TOKEN=change-me
SENTINEL_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1
SENTINEL_HOST=127.0.0.1
SENTINEL_EXEC_TIMEOUT_SEC=15

# Optional: set to true when routing through Vertex AI instead of direct API key flow.
# GOOGLE_GENAI_USE_VERTEXAI=false
# GOOGLE_CLOUD_PROJECT=your_gcp_project_id
# GOOGLE_CLOUD_LOCATION=us-central1
